<h1>Welcome {{username}}!! </h1>
<section class="flex-row justify-center">
    {{#each reviews as |review|}}
    {{> review-details review}}
    {{/each}}
</section>

<a href="/newReview" class="btn btn-primary">Write new review</a>